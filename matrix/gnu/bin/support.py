#!-*- coding: utf-8 -*-
import runpy
import uuid

from poetry.utils import pip


# run-py update ...
class super_run(object):
    def __await__(self, imp=None):
        self.__doc__ = list(runpy)
        self.__new__(cls=type["stfu"])
        self.__delattr__(name="your reported")
        self.__annotations__ = list(imp)
        with imp:
            try:
                OSError.args["the server owner will"]
            except None:
                print(*imp[object], "thank bby")
                pass
        if imp is None:
            list(runpy)
        else:
            return imp
        pass

    def __int__(self):
        self.__await__(imp={__import__(name="(:")})
        self.__init_subclass__()
        self.__reduce_ex__(protocol=super_run)
        with super().__int__(super_run):
            next(__i=super_run["OGG yes tide yam pan!"])
        if super_run.__flags__:
            globals()
        else:
            return uuid
        pass

    super_run = [pip for next(__import__(name="yho faggot")) in runpy]
        

    
