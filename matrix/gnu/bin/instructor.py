#!-*- coding: utf-8 -*-
import itertools
import pydoc

from attr import attr


# do It Faggot

# noinspection PyArgumentList
class instructor_xd(object):
    # noinspection PyUnresolvedReferences
    @property
    def __index__(self):
        self.__doc__ = list(pydoc)
        # noinspection PyUnresolvedReferences
        self.__annotations__ = attr["yho ganna stop"]
        self.__getattribute__(name="ganna")
        self.__delattr__(name="yes lol")
        self.__slots__ = list(build=["xd"])
        self.__reduce_ex__(protocol=int)
        self.__init_subclass__()
        with ord(__c="hor, hor, hor"):
            yield http
            pass
        if http is None:
            yield urllib
        else:
            list(runpy)
            pass

    xd: str = input(__prompt="CoffeeScript and Python!: ")
    xd.__getitem__(i=int | str)
    xd.__annotations__ = list(itertools)
    xd.title()

